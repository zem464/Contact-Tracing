# Use this file to close the program

# Import the gui
from ct_GUI import contact_tracingGUI

# Create root window and main module
if __name__ == "__main__": 
    ct_gui = contact_tracingGUI()
    ct_gui.mainloop()